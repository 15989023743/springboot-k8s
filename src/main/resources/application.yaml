server:
  port: 8080
  tomcat:
    basedir: "/"
    
spring:
  application:
    name: springboot-k8s

management:
  endpoint:
    restart:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  server:
    port: 8081
